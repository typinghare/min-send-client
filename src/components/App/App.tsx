/**
 * The only index component. This component will replace the <div id="root"></div> in the
 * index.html file.
 * @author James Chan
 */
export function App() {
    return (
        <div>
            <span>Hello world!</span>
        </div>
    )
}